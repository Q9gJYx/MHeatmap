[project]
name = "mheatmap"
version = "0.1.0"
description = "Reproducibility companion for the MHeatmap method"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
